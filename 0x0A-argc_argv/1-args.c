#include <stdio.h>
#include <stdlib.h>

/**
  * main - Entry Point
  *
  * @argc: the number of arguments
  * @argv: the vector of aargumnts
  *
  * Return: 0 Always (success)
  */
int main(int argc,  char __attribute__((unused)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
